<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <!-- <link rel="stylesheet" href="/dist/style.css"> -->
  </head>
  <body>
    <div class="flex flex-col p-10 *:m-auto">
      <h1 class="flex text-3xl font-bold underline">Hello world!' ' <p class=" text-red-500"><%=new Date().toLocaleTimeString()%></p> </h1>
      <h1 >welcome to backend ibovs  </h1>
      <% if(typeof name!=='undefined'){ %>
        <p>test if</p>
        <%=name %>
        <% } %>
      </div>
      <!-- <button type="button" onclick=handleOnClick() >handleOnClick</button> -->
      <div>
       <% if(typeof jsons!=='undefined'){ %>
        <% =jsons %>
        <% for( json in jsons ) { %>
         <p><%=json %></p>
        <% } }%>

     </div>



    <script type="" src="/dist/visibilitychange.js"></script>
    <!-- <script>function handleOnClick() {console.log("handleOnClick");location.reload();}</script> -->
  </body>
</html>
